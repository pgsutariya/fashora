{% render 'async-style', file: 'section-lookbook.css' %}

{%- style-%}
  #lookbook-{{ section.id }}  {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    #lookbook-{{ section.id }} {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
 
   #lookbook-{{ section.id }} .img-wrapper img {
    border-radius: {{ section.settings.image_border_radius | append: 'rem' }};
  }

  .lookbook-point-list-item button > span {
    animation: lookbook-point-animation-{{ section.id }} 2s infinite;
  }

  #lookbook-{{ section.id }} .lookbook-point-list-item button span {
    border-color: {{ section.settings.point_color }};
  }

  #lookbook-{{ section.id }} .lookbook-point-list-item button > span > span {
    background: {{ section.settings.point_color }};
    width: {{ section.settings.point_diameter | append: 'rem' }};
    height: {{ section.settings.point_diameter | append: 'rem' }};
  }

  @keyframes lookbook-point-animation-{{ section.id }} {
    0% { box-shadow: 0 0 0 0px rgba({{ section.settings.point_color | color_to_rgb | remove: 'rgb(' | remove: ')' }}, 0.25); }
    100% { box-shadow: 0 0 0 20px rgba({{ section.settings.point_color | color_to_rgb | remove: 'rgb(' | remove: ')' }}, 0); }
  }

  @media (max-width: 749px) {
    {% for block in section.blocks %}
      #lookbook-{{ section.id }} .lookbook-point-list-item:nth-child({{ forloop.index }}) button {
        left: {{ block.settings.mobile_pos_left }}%;
        top: {{ block.settings.mobile_pos_top }}%;
      }
    {% endfor %}
  }

  @media (min-width: 750px) {
    {% for block in section.blocks %}
      #lookbook-{{ section.id }} .lookbook-point-list-item:nth-child({{ forloop.index }}) button {
        left: {{ block.settings.desktop_pos_left }}%;
        top: {{ block.settings.desktop_pos_top }}%;
      }
    {% endfor %}
  }

  #lookbook-{{ section.id }}  .tooltip {
    background: {{ section.settings.tooltip_bg_color }};
    color: {{ section.settings.tooltip_color }};
    box-shadow: 0 0 2rem rgba({{ section.settings.tooltip_color | color_to_rgb | remove: 'rgb(' | remove: ')' }}, .2);
    width: {{ section.settings.tooltip_width | append: 'px' }};
    text-align: {{ section.settings.tooltip_text_align }};
  }
{%- endstyle -%}

<lookbook-section 
  id="lookbook-{{ section.id }}"
  class="lookbook color-{{ section.settings.color_scheme }} gradient {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">

  <div 
    class="section-header page-width" 
    style="text-align: {{ section.settings.heading_alignment }};">
    {%- if section.settings.heading != blank -%}
      <h2 
        id="lookbook-heading"
        class="title inline-richtext {{ section.settings.heading_size }}">
        {{ section.settings.heading }}
      </h2>
    {%- endif -%}
    {%- if section.settings.description != blank %}
      <div class="description rte">
        {{ section.settings.description }}
      </div>
    {% endif %}
  </div>

  <div 
    class="{% if section.settings.container %}page-width{% endif %}"
    style="max-width: {{ section.settings.container_max_width | append: 'px' }}">

    <div class="lookbook-inner">

      <div class="img-wrapper img-wrapper-mobile">
        {% if section.settings.mobile_image == nil %}
          {{ 'lifestyle-1' | placeholder_svg_tag }}
        {% else %}
          <img
            src="{{ section.settings.mobile_image.src | image_url: width: 800 }}"
            alt="{{ section.settings.mobile_image.alt | escape }}"
            class=""
            width="800"
            height="{{ 600 | divided_by: section.settings.mobile_image.aspect_ratio | round }}"
            loading="lazy">
        {% endif %}
      </div>
      <div class="img-wrapper img-wrapper-desktop">
        {% if section.settings.desktop_image == nil %}
          {{ 'lifestyle-1' | placeholder_svg_tag }}
        {% else %}
          <img
            src="{{ section.settings.desktop_image.src | image_url: width: 1600 }}"
            alt="{{ section.settings.desktop_image.alt | escape }}"
            class=""
            width="1600"
            height="{{ 1600 | divided_by: section.settings.desktop_image.aspect_ratio | round }}"
            loading="lazy">
        {% endif %}
      </div>

      <div 
        class="lookbook-point-list" 
        aria-labelledby="lookbook-heading"
        role="list">
        {% for block in section.blocks %}
          <div 
            class="lookbook-point-list-item" 
            {{ block.shopify_attributes }}
            role="listitem">
            {% liquid
              assign product = block.settings.product 
            %}
            <button>
              <span><span>
              </span></span>
            </button>
            <div class="tooltip" role="tooltip">
              {% unless block.settings.title == blank %}
                <h3 class="title">
                  {{ block.settings.title }}
                </h3>
              {% endunless %}
              {% if product != nil and block.settings.show_image %} 
                  <img 
                    class="lookbook-product-img" 
                    src="{{ product.featured_image | image_url: width: 600 }}" 
                    width="600"
                    height="{{ 600 | divided_by: product.featured_image.aspect_ratio | round }}" 
                    loading="lazy">
              {% endif %}
              {% if product != nil and block.settings.show_title %}
                <div class="product-title">
                  {{ product.title }}
                </div>
              {% endif %}
              {% if product != nil and block.settings.show_price %} 
                <p class="lookbook-product-price">
                  {% if product.compare_at_price > product.price %}
                    <span class="lookbook-product-price-compare">
                      <span class="visually-hidden visually-hidden--inline">
                        {{ 'products.product.price.regular_price' | t }}
                      </span>
                      <s>{{ product.compare_at_price | money }}</s>
                    </span>
                  {% endif %}
                  <span class="lookbook-product-price-final">
                    {{ product.price | money }}
                  </span>
                </p>
              {% endif %}
              {% if block.settings.description != blank %}
                <div class="description rte">
                  {{ block.settings.description }}
                </div>
              {% endif %}
              {% if product != nil and block.settings.btn_text != blank %} 
                <a 
                  class="button {{ section.settings.tooltip_btn_style }}"
                  href="{{ product.url }}"
                  aria-hidden="true">
                  {{ block.settings.btn_text }}
                </a>
              {% endif %}
            </div>
          </div>
        {% endfor %}
      </div>

    </div>
  </div>
</lookbook-section>

{%- javascript -%} 
  class LookBook extends HTMLElement {
    constructor () {
      super()

      this.injectVendorScripts()
      this.querySelectorAll('.lookbook-point-list-item').forEach((elem) => {
        this.handleTooltip(elem)
      })
    } 
    
    async injectVendorScripts () {
      if (!window.FloatingUICore) {
        const script = document.createElement('script')
        script.setAttribute('src', 'https://cdn.jsdelivr.net/npm/@floating-ui/core@1.6.0')
        document.head.appendChild(script) 
      }
      if (!window.FloatingUIDOM) {
        await new Promise(r => setTimeout(r, 500))

        const script = document.createElement('script')
        script.setAttribute('src', 'https://cdn.jsdelivr.net/npm/@floating-ui/dom@1.6.1')
        document.head.appendChild(script)
      }
    }

    handleTooltip(elem) {
      const btn = elem.querySelector('button')
      const tooltip = elem.querySelector('.tooltip')

      function update() {
        window.FloatingUIDOM.computePosition(btn, tooltip, {
          middleware: [
            window.FloatingUIDOM.offset(0),
            window.FloatingUIDOM.autoPlacement()
          ],
        }).then(({x, y}) => {
          Object.assign(tooltip.style, {
            left: `${x}px`,
            top: `${y}px`,
          })
        })
      }

      function showTooltip() {
        tooltip.classList.add('show')
        update()
      }
      
      function hideTooltip() {
        setTimeout(() => {
          tooltip.classList.remove('show')
          tooltip.classList.add('hiding')
          setTimeout(() => {
            tooltip.classList.remove('hiding')
          }, 200)
        }, 100)
      }
      
      if (window.matchMedia("(pointer: coarse)").matches) {
        [
          ['mouseenter', showTooltip],
          ['mouseleave', hideTooltip],
          ['focus', showTooltip],
          ['blur', hideTooltip],
        ].forEach(([event, listener]) => {
          btn.addEventListener(event, listener)
        })
      } else {
        [
          ['focus', showTooltip],
          ['blur', hideTooltip],
        ].forEach(([event, listener]) => {
          btn.addEventListener(event, listener)
        })
      }
    }
  }
  customElements.define('lookbook-section', LookBook)
{%- endjavascript -%}


{% schema %}
{
  "name": "t:sections.lookbook.name",
  "tag": "section",
  "class": "section section-lookbook",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.lookbook.settings.color_scheme.label",
      "default": "background-1"
    },
    {
      "type": "checkbox",
      "id": "container",
      "label": "t:sections.lookbook.settings.container.label",
      "default": true
    },
    {
      "type": "text",
      "id": "container_max_width",
      "label": "t:sections.lookbook.settings.container_max_width.label",
      "info": "t:sections.lookbook.settings.container_max_width.info"
    },
    {
      "type": "header",
      "content": "t:sections.lookbook.settings.header.content"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Lookbook"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "t:sections.all.heading_size.options__1.label"
        },
        {
          "value": "h1",
          "label": "t:sections.all.heading_size.options__2.label"
        },
        {
          "value": "h0",
          "label": "t:sections.all.heading_size.options__3.label"
        },
        {
          "value": "hxl",
          "label": "t:sections.all.heading_size.options__4.label"
        },
        {
          "value": "hxxl",
          "label": "t:sections.all.heading_size.options__5.label"
        }
      ],
      "default": "h1",
      "label": "t:sections.all.heading_size.label"
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "t:sections.collapsible_content.settings.heading_alignment.label",
      "options": [
        {
          "value": "left",
          "label": "t:sections.collapsible_content.settings.heading_alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.collapsible_content.settings.heading_alignment.options__2.label"
        },
        {
          "value": "right",
          "label": "t:sections.collapsible_content.settings.heading_alignment.options__3.label"
        }
      ],
      "default": "left"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "t:sections.lookbook.settings.description.label",
      "default": "t:sections.lookbook.settings.description.default",
      "info": "t:sections.lookbook.settings.description.info"
    },
    {
      "type": "header",
      "content": "t:sections.lookbook.settings.header.image"
    },
    {
      "type": "image_picker",
      "id": "mobile_image",
      "label": "Mobile Image"
    },
    {
      "type": "image_picker",
      "id": "desktop_image",
      "label": "t:sections.lookbook.settings.desktop_image.label"
    },
    {
      "type": "range",
      "id": "image_border_radius",
      "label": "t:sections.lookbook.settings.image_border_radius.label",
      "min": 0,
      "max": 5,
      "step": 0.2,
      "default": 1,
      "unit": "rem"
    },
    {
      "type": "header",
      "content": "t:sections.lookbook.settings.header.points"
    },
    {
      "type": "color",
      "id": "point_color",
      "label": "t:sections.lookbook.settings.point_color.label",
      "default": "#334fb4"
    },
    {
      "type": "range",
      "id": "point_diameter",
      "label": "t:sections.lookbook.settings.point_diameter.label",
      "unit": "rem",
      "min": 0.2,
      "max": 3,
      "step": 0.1,
      "default": 1
    },
    {
      "type": "header",
      "content": "t:sections.lookbook.settings.header.tooltip"
    },
    {
      "type": "range",
      "id": "tooltip_width",
      "label": "t:sections.lookbook.settings.tooltip_width.label",
      "default": 260,
      "min": 200,
      "max": 400,
      "step": 20,
      "unit": "px"
    },
    {
      "type": "select",
      "id": "tooltip_text_align",
      "label": "t:sections.lookbook.settings.tooltip_text_align.label",
      "default": "center",
      "options": [
        {
          "value": "",
          "label": "t:sections.lookbook.settings.tooltip_text_align.options.left.label"
        },
        {
          "value": "center",
          "label": "t:sections.lookbook.settings.tooltip_text_align.options.center.label"
        }
      ]
    },
    {
      "type": "color",
      "id": "tooltip_bg_color",
      "label": "t:sections.lookbook.settings.tooltip_bg_color.label",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "tooltip_color",
      "label": "t:sections.lookbook.settings.tooltip_color.label",
      "default": "#121212"
    },
    {
      "type": "select",
      "id": "tooltip_btn_style",
      "label": "t:sections.lookbook.settings.tooltip_btn_style.label",
      "options": [
        {
          "value": "",
          "label": "t:sections.lookbook.settings.tooltip_btn_style.options.primary.label"
        },
        {
          "value": "button--secondary",
          "label": "t:sections.lookbook.settings.tooltip_btn_style.options.secondary.label"
        }
      ],
      "default": ""
    },
    {
      "type": "header",
      "content": "t:sections.lookbook.settings.header.section_padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.lookbook.settings.padding_top.label",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.lookbook.settings.padding_bottom.label",
      "default": 36
    },
    {
      "type": "header",
      "content": "t:sections.lookbook.settings.header.get_support",
      "info": "t:sections.lookbook.settings.header.get_support_info"
    },
    {
      "type": "header",
      "content": "t:sections.lookbook.settings.header.shopify_snippets",
      "info": "t:sections.lookbook.settings.header.shopify_snippets_info"
    },
    {
      "type": "header",
      "content": "t:sections.lookbook.settings.header.shopify_themes",
      "info": "t:sections.lookbook.settings.header.shopify_themes_info"
    }
  ],
  "blocks": [
    {
      "type": "point",
      "name": "t:sections.lookbook.blocks.point.name",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "t:sections.lookbook.blocks.point.settings.title.label",
          "default": "t:sections.lookbook.blocks.point.settings.title.default"
        },
        {
          "type": "inline_richtext",
          "id": "description",
          "label": "t:sections.lookbook.blocks.point.settings.description.label",
          "default": "t:sections.lookbook.blocks.point.settings.description.default",
          "info": "t:sections.lookbook.blocks.point.settings.description.info"
        },
        {
          "type": "header",
          "content": "t:sections.lookbook.blocks.point.settings.header.product",
          "info": "t:sections.lookbook.blocks.point.settings.header.product_info"
        },
        {
          "type": "product",
          "id": "product",
          "label": "t:sections.lookbook.blocks.point.settings.product.label"
        },
        {
          "type": "checkbox",
          "id": "show_image",
          "label": "t:sections.lookbook.blocks.point.settings.show_image.label",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_title",
          "label": "t:sections.lookbook.blocks.point.settings.show_title.label",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_price",
          "label": "t:sections.lookbook.blocks.point.settings.show_price.label",
          "default": true
        },
        {
          "type": "text",
          "id": "btn_text",
          "label": "t:sections.lookbook.blocks.point.settings.btn_text.label",
          "default": "t:sections.lookbook.blocks.point.settings.btn_text.default"
        },
        {
          "type": "header",
          "content": "t:sections.lookbook.blocks.point.settings.header.point_position_mobile"
        },
        {
          "type": "range",
          "id": "mobile_pos_left",
          "label": "t:sections.lookbook.blocks.point.settings.mobile_pos_left.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0
        },
        {
          "type": "range",
          "id": "mobile_pos_top",
          "label": "t:sections.lookbook.blocks.point.settings.mobile_pos_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0
        },
        {
          "type": "header",
          "content": "t:sections.lookbook.blocks.point.settings.header.desktop_positioning"
        },
        {
          "type": "range",
          "id": "desktop_pos_left",
          "label": "t:sections.lookbook.blocks.point.settings.desktop_pos_left.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0
        },
        {
          "type": "range",
          "id": "desktop_pos_top",
          "label": "t:sections.lookbook.blocks.point.settings.desktop_pos_top.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 0
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.lookbook.presets.name"
    }
  ]
}
{% endschema %}